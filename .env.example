# URL of the Google Apps Script Web App deployment (see scripts/apps-script-instagram-intake.gs).
# Public by nature (this is a static site, baked into the client JS bundle at build time) —
# safety comes from the endpoint re-validating input server-side, not from hiding this URL.
NEXT_PUBLIC_IG_ENDPOINT=
